#include <stdio.h>
#include <stdlib.h>
//Dictionary Lookup


void addData(char item[20]){


}

int main(){
    int size;   
    scanf("%d", &size);
    char data[size][20];
    for(int i = 0; i < size; i++){
        scanf("%s", data[i]);
    }
    return 0;
}